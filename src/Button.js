export default function Button() {
    return (
        <div className={"btn"}>
            <button className="active">Activate Post</button>
            <button className="inactive">inactivate Post</button>
        </div>
    );
}

